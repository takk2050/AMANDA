import { AxiosInstance } from 'axios';
import { AuthData, Keystore, OauthClientRequest, Scope } from '../types';
export declare class OauthClient implements OauthClientRequest {
    keystore: Keystore;
    request: AxiosInstance;
    newUUID: () => string;
    authorizeToken(code: string, client_secret?: string, code_verifier?: string): Promise<{
        access_token: string;
        scope: string;
    }>;
    getAuthorizeCode(params: {
        client_id: string;
        scopes?: Scope[];
        pin?: string;
    }): Promise<AuthData>;
    getAuthorizeData(client_id: string, _scope?: Scope[]): Promise<AuthData>;
}
